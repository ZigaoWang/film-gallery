{
  "name": "film-gallery",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "//postinstall": "Regenerates the Prisma client on every install. Without this the deploy script's `npm install && npm run build` builds against whatever client happened to be left in node_modules, so the first deploy after a schema change fails the type check on models that plainly exist.",
    "postinstall": "prisma generate",
    "//prebuild": "Clears the incremental build cache before every build. A change to globals.css once shipped to production without its stylesheet: the source was correct on the server and on the right commit, but the cache handed the build an old CSS chunk, so the deploy reported success and served the previous styles. A silent wrong result is worth more than the twenty seconds this costs. It was `rm -rf .next/cache`, which also deleted the image optimizer cache — see scripts/clear-build-cache.mjs.",
    "prebuild": "node scripts/clear-build-cache.mjs",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "tsx scripts/test/enumLabels.test.ts && tsx scripts/test/sameOriginPath.test.ts && tsx scripts/test/passageSupports.test.ts && tsx scripts/test/catalogueWrites.test.ts && tsx scripts/test/rateLimit.test.ts && tsx scripts/test/feedScope.test.ts && tsx scripts/test/profileView.test.ts && tsx scripts/test/linkify.test.tsx && tsx scripts/test/sharpPipeline.test.ts && tsx scripts/test/duplicateDetection.test.ts && tsx scripts/test/blocks.test.ts && tsx scripts/test/feedback.test.ts && tsx scripts/test/exifStrip.test.ts && tsx scripts/test/ownerScopedWrites.test.ts && tsx scripts/test/catalogForm.test.ts && tsx scripts/test/gearSpecs.test.ts",
    "postbuild": "node scripts/assert-client-bundle-clean.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.958.0",
    "@prisma/client": "^6.19.1",
    "@types/qrcode": "^1.5.6",
    "bcryptjs": "^3.0.3",
    "blurhash": "^2.0.5",
    "canvas": "^3.2.1",
    "exifr": "^7.1.3",
    "heic-decode": "^2.1.0",
    "heic2any": "^0.0.4",
    "marked": "^18.0.11",
    "next": "^16.3.2",
    "next-auth": "^4.24.13",
    "prisma": "^6.19.1",
    "qrcode": "^1.5.4",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.3.2",
    "piexifjs": "^1.0.6",
    "tailwindcss": "^4",
    "tsx": "^4.23.12",
    "typescript": "^5"
  }
}
